  // **********Primitive ************

  // no. =>  2 to pwr 53 range
  // bigint => for larger values
  // String => ""
  // boolean => true || false
  // null => standalone value (iska matlab hai empty or zero nahi hai)
  // undefined => (aapne variable declare kardiya hai but usmein aap kya value dalenge aapne decide nahi kiya hai)
  // symbol => unique (kisi bhi value ko unique banane ke liye use kiya jata hai)

  // ************* Preference datatype || Non Primitive *************
  //  1. Array ( [ ] )
  //  2. Object ( { } )
  //  3. Function ( function( ){ } )

  let age = 3

  console.log(typeof "Dipali")
  console.log(typeof age)

  // imp - asked in interveiw
  console.log(typeof null) // object
  console.log(typeof undefined) // undefined
